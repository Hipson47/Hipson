# Optional fallback provider env.
# Prefer copying .env.example to repo-root .env for local development.
# Use this file as a template for ~/.config/hipson/agents.env when you want
# one user-level sidecar config shared across many repos.
# Do not commit real keys.

OPENROUTER_API_KEY=
